import React from 'react';
import PropTypes from 'prop-types';

MyPageTableTemplate.propTypes = {
    
};

function MyPageTableTemplate(props) {
    return (
        <div>
            
        </div>
    );
}

export default MyPageTableTemplate;